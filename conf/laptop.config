/*
 * ---------------------------------------------------
 *  Nextflow config file for running tests on a laptop
 *  or other local device
 * ---------------------------------------------------
 */

params {
    max_cpus   = 4
    max_memory = 8.GB
}
